streamlit>=1.35.0
pillow>=10.0.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# opencv-python (GUI build) is uninstalled at runtime by app.py before cv2 loads.
# Only the headless build is used at runtime.
opencv-python-headless>=4.8.0

# CPU-only torch
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0
huggingface_hub>=0.20.0

python-dotenv>=1.0.0

# Gemini Vision API
google-genai>=1.0.0
