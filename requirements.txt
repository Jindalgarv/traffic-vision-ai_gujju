streamlit>=1.35.0
pillow>=10.0.0
pandas>=2.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
requests>=2.31.0

# Local YOLO inference (no API key required)
# Downloads yolov8n.pt from ultralytics CDN, helmet + LP models from HuggingFace Hub.
ultralytics>=8.0.0
huggingface_hub>=0.20.0

python-dotenv>=1.0.0

# Gemini Vision API (Hybrid AI mode)
google-genai>=1.0.0

# OCR option 1 – recommended (real OCR instead of mock fallback):
# easyocr>=1.7.2

# OCR option 2 – PaddleOCR (may need matching paddlepaddle build):
# paddleocr>=3.0.0
# paddlepaddle>=3.0.0
