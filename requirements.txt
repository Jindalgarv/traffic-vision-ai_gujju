streamlit>=1.35.0
pillow>=10.0.0
pandas>=2.0.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
requests>=2.31.0
inference-sdk>=0.59.0

python-dotenv>=1.0.0
# OCR option 1. Recommended first because the app can call easyocr.Reader().readtext().
# Install when you want real OCR instead of the mock fallback:
# easyocr>=1.7.2

# OCR option 2. PaddleOCR may also require a matching paddlepaddle install for your OS/CPU/GPU.
# paddleocr>=3.0.0
# paddlepaddle>=3.0.0
